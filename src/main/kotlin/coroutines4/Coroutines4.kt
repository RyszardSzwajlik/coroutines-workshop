package coroutines4

/*
    Decompile using `javap corotunes4.Coroutines4`.
    Try with and without `suspend` keyword
 */
class Coroutines4 {
    suspend fun getName(name: String): String {
        return name
    }
}